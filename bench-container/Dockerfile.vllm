# =============================================================================
# GPUSCALE Benchmark Container — vLLM engine
# Supports: HuggingFace format models (safetensors, GPTQ)
# ~5-6 GB compressed (PyTorch + vLLM)
# NOT CURRENTLY BUILT — uncomment in build-bench.yml when needed
# =============================================================================

# FROM nvidia/cuda:12.4.1-runtime-ubuntu22.04
#
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     python3 python3-pip python3-venv \
#     jq curl \
#     && rm -rf /var/lib/apt/lists/*
#
# COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv
#
# COPY requirements.vllm.txt /tmp/requirements.txt
# RUN uv pip install --system --no-cache -r /tmp/requirements.txt
#
# WORKDIR /app
# COPY entrypoint.sh /app/entrypoint.sh
# COPY scripts/ /app/scripts/
# COPY workloads/ /app/workloads/
#
# RUN chmod +x /app/entrypoint.sh /app/scripts/*.sh
# RUN mkdir -p /models /results
#
# ENTRYPOINT ["/app/entrypoint.sh"]
